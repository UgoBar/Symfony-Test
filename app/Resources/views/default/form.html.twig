{% extends 'default/index.html.twig' %}

{% block content %}

    {{ form_start(form) }}
    {{ form_rest(form) }}
    {{ form_end(form) }}

    {% if app.request.method == 'POST' and data %}
        good job !
    {% else %}
        submit something please
    {% endif %}
{% endblock %}